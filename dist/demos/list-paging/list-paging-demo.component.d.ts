import { OnInit } from '@angular/core';
import { ListStateDispatcher } from '../../core';
export declare class SkyListPagingDemoComponent implements OnInit {
    private dispatcher;
    constructor(dispatcher: ListStateDispatcher);
    ngOnInit(): void;
}
